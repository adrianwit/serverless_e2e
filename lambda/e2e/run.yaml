init:
  app: bqwt
  target:
    URL: ssh://127.0.0.1/
    credentials: localhost
  appHost: $Hostname($target.URL)
  awsCredentials: aws
  awsSecrets: $secrets.aws
  appPath: $WorkingDirectory(../)
defaults:
  app: $app
  target: $target
  buildTarget: $buildTarget
  appTarget: $appTarget
  bqCredentials: $bqCredentials
pipeline:
  test:
    action: run
    request: '@regression/regression'
    tasks: '*'
